name: Keep Alshefaa Alive

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase API
        run: curl -s https://yedflaybetuzribvcsmk.supabase.co/rest/v1/
